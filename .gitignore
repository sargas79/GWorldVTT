node_modules/
dist/
coverage/
foundry-config.json
*.log
.DS_Store
Thumbs.db

# Release artifact, built on demand
gworld.zip

# Compiled compendium packs are a build artifact
dist/packs/

# Parser reject log, regenerated on each run
packs-src/*/.rejected*.txt

# Obsidian vault settings, if the folder is opened as a vault
.obsidian/

# The knowledge graph is a derived artifact: 2MB of JSON rebuilt by the
# post-commit hook on every commit, plus a machine-specific interpreter path in
# .graphify_python. Each clone builds its own with `graphify update .` (AST
# only, no API cost).
graphify-out/

# Local render harness output (screenshots for eyeballing layout).
.harness/
